/**
 * @module config
 * @description Global configuration settings for the JKØ application.
 */
"use strict";

export const CONFIG = {
    API_BASE_URL: '/api/v1',
    TIMEOUT: 5000,
    CURRENCY_SYMBOL: '$',
    LANGUAGE: 'en-US'
};
